{
  "name": "primit-grid-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "cd frontend && bun install",
    "build": "cd frontend && bun run build",
    "start": "cd frontend && bun run build && cd .. && bun run backend/src/index.ts",
    "dev": "cd frontend && bun run build && cd .. && bun run --watch backend/src/index.ts"
  }
}
